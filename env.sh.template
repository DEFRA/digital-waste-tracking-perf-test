#!/bin/sh
# Usage: source ./env.sh
# Copy this file to env.sh and fill in your values

# Common variables

export TEST_SCENARIO=all  # "all" for all tests, or specific JMX file path
# export TEST_SCENARIO=create-waste-movement/successfully/baseline-test.jmx
export CI=false  # Set to true in CDP Platform, uploads report to S3
export DEBUG=false  # Set to true to log request/response details to jmeter-test log

# Environment variables

export COGNITO_CLIENT_ID=  # OAuth2 client ID from Cognito user pool
export COGNITO_CLIENT_SECRET=  # OAuth2 client secret from Cognito user pool
export COGNITO_OAUTH_BASE_URL=  # Cognito OAuth2 base URL for the user pool
export ENVIRONMENT=  # Target environment: dev, test, perf-test, ext-test, prod
export API_CODE=  # Organisation API code for waste movement requests

# CI ONLY

# export HTTP_PROXY=http://proxy.example.com:8080  # HTTP proxy (format: http://host:port)
